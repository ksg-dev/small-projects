# # keyword breakdown
# #new_list = [new_item for item in list if test]
#
# names = ["Alex", "Beth", "Caroline", "Dave", "Eleanor", "Freddie"]
#
# # say only want names with 4 letters or less
# short_names = [name for name in names if len(name) < 5]
#
# # Now say we want names w more than 5 characters to be added to list in uppercase
# upper = [name.upper() for name in names if len(name) > 4]
#
# print(f"short_names: {short_names}")
# print(f"upper: {upper}")

